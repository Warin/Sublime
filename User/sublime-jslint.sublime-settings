{
    // Options pass to jslint.
    "jslint_options": "--white --vars --sloppy --nomen",

    // Ignore errors, regex.
    "ignore_errors":
    [
        // "Expected an identifier and instead saw 'undefined' \(a reserved word\)"
    ],

    // run jslint on save.
    "run_on_save": false,

    // debug flag.
    "debug": false
}
